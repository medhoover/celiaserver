{% extends "MedCeliaBundle:Main:template.html.twig" %}
{% block estrecette %} active {% endblock %}
{% block titre %} Recettes {% endblock %}
{% block contenue %}
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <div class="text-muted bootstrap-admin-box-title">Table De Recettes</div>
            </div>
            <div class="bootstrap-admin-panel-content">
                <table class="table bootstrap-admin-table-with-actions">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Nom</th>
                            <th>Categorie</th>
                            <th>Modifier</th>
                            <th>Supprimer</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for recette in recettes %}
                            <tr>
                                <td>{{recette.id}}</td>
                                <td>{{recette.nom}}</td>
                                <td>{{recette.categorie.nom}}</td>
                                <td> 
                                    <a href="{{ path('med_celia_view_recette', {id:recette.id}) }}">
                                        <button class="btn btn-sm btn-primary">
                                             <i class="glyphicon glyphicon-pencil"></i>
                                             Modifier
                                        </button>
                                    </a>
                                </td>
                                <td> 
                                    <a href="{{ path('med_celia_delete_recette', {id:recette.id}) }}">
                                        <button class="btn btn-sm btn-danger">
                                             <i class="glyphicon glyphicon-trash"></i>
                                             Supprimer
                                        </button>
                                    </a>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>

{% endblock %}